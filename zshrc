# Matt Williamson's zsh config

export EDITOR=vim

# Fix vim colors
export TERM=xterm-256color

# Aliases
alias l='ls -A'
alias la='ls -la'
alias gsh='git show'
alias src='source ~/.zshrc'
alias glg='git log --stat --max-count=3'
alias glgg='git log --graph --max-count=3'
